---
// src/components/BrushAccent.astro
interface Props {
  text: string;
  class?: string;
}

const { text, class: className = '' } = Astro.props;
---

<span class={`brush-accent ${className}`}>{text}</span>